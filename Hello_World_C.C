#include <stdio.h>//This is to include the library stdio.h to input not put to input text to the terminal

int main(void)//Defining a function called main for integers but void makes sure it doesnt take any arguements/parameters
{
    printf("Hello World!\n");
    return 0;
}
//return 0 is the standard return value from the main function. Its telling the system that the program returned with a status of zero.
//printf is calling the function(telling the function to do some work for us). In this case output to the screen hello world.
//semi colons end statements